# ─────────────────────────────────────────────────────────────────────────────
# Local Docker dev environment
#
# Copy this file to .env.development.local  (already gitignored via *.local)
# then run:  docker compose up -d  &&  npm run dev
#
# Vite will automatically proxy /api  →  http://localhost:8080
# so no CORS configuration is needed.
# ─────────────────────────────────────────────────────────────────────────────

# Relative path → activates the Vite proxy in vite.config.ts
VITE_API_URL=/api
