# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# PHP – keep config.example.php but not the real config with credentials
api/config.php

# User-uploaded files
api/uploads/*
!api/uploads/.htaccess

# FTP deploy sync state (lives on server, not in repo)
.ftp-deploy-sync

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
